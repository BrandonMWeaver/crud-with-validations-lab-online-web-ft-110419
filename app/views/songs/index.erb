<h1>Songs</h1>
<table>
<% @songs.each do |song| %>
  <tr>
    <th>Title</th>
    <th>Artist Name</th>
    <th>Genre</th>
  </tr>
  <tr>
    <td><%= link_to song.title, song_path(song) %></td>
    <td><%= @song.artist_name %></td>
    <td><%= @song.genre %></td>
  </tr>
<% end %>
</table>
