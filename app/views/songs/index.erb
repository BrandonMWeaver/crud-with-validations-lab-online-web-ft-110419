<h1>Songs</h1>
<table>
<% @songs.each do |song| %>
  <tr>
    <th>Song</th>
    <th>Release Year</th>
    <th>Artist Name</th>
    <th>Genre</th>
  </tr>
  <tr>
  <td><%= link_to song.title, song_path(song) %></td>
  </tr>
<% end %>
</table>
